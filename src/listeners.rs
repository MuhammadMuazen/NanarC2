pub fn setup_socket_listener(local_host: &str, local_port: &str) {

    println!("[i] Setting up socket listener...");
}

pub fn setup_http_listener(local_host: &str, local_port: &str) {

    println!("[i] Setting up HTTP listener...")
}


pub fn setup_https_listener(local_host: &str, local_port: &str, certificate_path: &str) {

    println!("[i] Setting up HTTPS listener...")
}

pub fn setup_nanar_revshell_socket(local_host: &str, local_port: &str) {

    println!("[i] Setting up nanar rev shell socket listener...")
}

pub fn setup_nanar_revshell_http(local_host: &str, local_port: &str) {

    println!("[i] Setting up nanar rev shell HTTP listener...")
}

pub fn setup_nanar_revshell_https(local_host: &str, local_port: &str, certificate_path: &str) {

    println!("[i] Setting up nanar rev shell HTTPS listener...")
}